pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url = uri("https://jitpack.io") }
    }
}
rootProject.name = "ToolKit"
include ':app'
include ':core' // TODO <- Delete
include ':compose:ui:theme'
include ':compose:ui:components'
include ':compose:base-arch'
include ':base:logger'
include ':base:test'
include ':base:extensions'
